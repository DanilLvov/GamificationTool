extends RefCounted
class_name UIConst

const BG_SIZE                 := Vector2(1920, 1080)

# --- Margins ---
const MARGIN_DEFAULT          := 15
const MARGIN_PANEL            := 40
const MARGIN_PANEL_BOTTOM     := 20

# --- Spacing ---
const SEPARATION_DEFAULT      := 20

# --- Corner radii ---
const CORNER_RADIUS_DEFAULT   := 12
const CORNER_RADIUS_BUTTON    := 10

# --- Texture margins (StyleBoxTexture) ---
const TEXTURE_MARGIN          := 35

# --- Button sizes ---
const BUTTON_SIZE_STD         := Vector2(200, 30)
const BUTTON_SIZE_SMALL       := Vector2(120, 50)
const BUTTON_SIZE_DEBUG       := Vector2(120, 50)

# --- Default sizes ---
const ORDERING_CARD_SIZE      := Vector2(50, 50)
const COLORED_PANEL_SIZE      := Vector2(50, 50)

# --- Panel sizes ---
const PLANET_PANEL_SIZE       := Vector2(250, 250)

# --- Line number column width ---
const LINE_NUMBER_WIDTH       := 36

# --- Code snippet ---
const CODE_SNIPPET_HEADER_HEIGHT := 42
const CODE_SNIPPET_FONT_SIZE     := 16
const CODE_SNIPPET_BORDER_WIDTH  := 1

# --- Code snippet colors ---
const COLOR_CODE_BG           := Color8(18, 22, 38)
const COLOR_CODE_BORDER       := Color8(55, 65, 95)
const COLOR_CODE_ACCENT       := Color8(0, 216, 177)
const COLOR_CODE_FILENAME     := Color8(170, 175, 195)
const COLOR_CODE_LINE_NUMBERS := Color8(130, 135, 155)
const COLOR_CODE_TEXT         := "#d8e0ff"

# --- Colored panel defaults ---
const COLORED_PANEL_DEFAULT_COLOR  := Color8(0, 216, 177)
const COLORED_PANEL_DEFAULT_RADIUS := 10
